{
  "name": "trading-dashboard-sync",
  "version": "1.0.0",
  "description": "Trading data sync cron job",
  "main": "scripts/renderCronSync.js",
  "type": "module",
  "scripts": {
    "sync": "node scripts/renderCronSync.js",
    "test": "node --version && npm list"
  },
  "dependencies": {
    "google-spreadsheet": "^4.1.2",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
